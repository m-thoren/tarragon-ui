---
import ComponentLayout from '@/layouts/ComponentLayout.astro'
import { buildUrl } from '@/scripts/constants'
---

<ComponentLayout name='Gap'>
	<p>
		Combine the <code>.gap-{`{size}`}</code> class with the <a href={buildUrl('/size')}>
			size values
		</a>
	</p>
</ComponentLayout>
